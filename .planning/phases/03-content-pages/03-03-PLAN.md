---
phase: 03-content-pages
plan: 03
type: execute
wave: 3
depends_on: ["03-01"]
files_modified:
  - src/content.config.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Content collection uses Astro 5 Content Layer API with explicit glob loader"
    - "Loader pattern matches actual file extensions (.md files)"
  artifacts:
    - path: "src/content.config.ts"
      provides: "Content Layer configuration with pages collection and explicit loader"
      min_lines: 15
      contains: "loader"
  key_links:
    - from: "src/content.config.ts"
      to: "src/content/pages/"
      via: "glob loader"
      pattern: "glob.*content/pages"
---

<objective>
Add explicit glob loader to content.config.ts to comply with Astro 5 Content Layer API specification.

Purpose: The current implementation works due to Astro's implicit loading, but the explicit loader is the documented Astro 5 pattern and provides better control over content loading.
Output: Updated content.config.ts with glob loader matching the actual .md file extensions.
</objective>

<execution_context>
@/home/mcrowe/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Gap Context
The verification found that content.config.ts is missing the required 'loader' property with glob() as specified in plan 03-01. The plan originally specified .mdx extension, but decision "MD over MDX" (03-02) established using .md files instead.

# Current Implementation
@src/content.config.ts
@src/content/pages/privacy.md
@src/content/pages/terms.md
@src/pages/privacy.astro
@src/pages/terms.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit glob loader to content.config.ts</name>
  <files>src/content.config.ts</files>
  <action>
Update the content.config.ts file to add the explicit glob loader for the pages collection.

The current implementation has:
```typescript
import { defineCollection, z } from 'astro:content';

const pages = defineCollection({
  schema: z.object({
    title: z.string(),
    description: z.string(),
    lastUpdated: z.coerce.date().optional(),
  }),
});

export const collections = {
  pages,
};
```

Update it to:
```typescript
import { defineCollection, z } from 'astro:content';
import { glob } from 'astro/loaders';

const pages = defineCollection({
  loader: glob({ pattern: '**/*.md', base: './src/content/pages' }),
  schema: z.object({
    title: z.string(),
    description: z.string(),
    lastUpdated: z.coerce.date().optional(),
  }),
});

export const collections = {
  pages,
};
```

IMPORTANT:
- Import `glob` from `astro/loaders`
- Use `.md` pattern (NOT .mdx) to match the actual file extensions and honor the "MD over MDX" decision
- The loader property goes BEFORE the schema property
  </action>
  <verify>
    ```bash
    # Verify glob loader is present and uses .md pattern
    grep -q "import { glob } from 'astro/loaders'" src/content.config.ts && \
    grep -q "loader: glob" src/content.config.ts && \
    grep -q "\.md" src/content.config.ts && \
    npm run build && \
    echo "Content config with glob loader valid"
    ```
  </verify>
  <done>Content Layer configuration has explicit glob loader using .md pattern matching the actual content files. Build succeeds without errors.</done>
</task>

</tasks>

<verification>
1. Run `npm run build` to verify content collection compiles with explicit loader
2. Verify the loader pattern matches actual file extensions (.md)
3. Verify Privacy and Terms pages still render correctly
4. Confirm no regressions in content querying
</verification>

<success_criteria>
- [ ] content.config.ts has explicit glob loader with import from 'astro/loaders'
- [ ] Loader pattern uses **/*.md to match actual content files
- [ ] Build succeeds without errors
- [ ] Privacy and Terms pages still render content correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-pages/03-03-SUMMARY.md`
</output>
