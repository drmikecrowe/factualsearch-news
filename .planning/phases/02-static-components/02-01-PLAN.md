---
phase: 02-static-components
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/layouts/BaseLayout.astro
  - src/pages/index.astro
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User sees consistent header with logo on every page"
    - "User sees navigation links in header (Home, About, Embed, Terms, Privacy, Sites)"
    - "User sees footer with same navigation links on every page"
    - "User sees MBFC attribution in footer"
    - "User sees copyright year in footer (dynamic, updates automatically)"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Site header with logo and navigation"
      min_lines: 20
    - path: "src/components/Footer.astro"
      provides: "Site footer with links and attribution"
      min_lines: 30
    - path: "src/layouts/BaseLayout.astro"
      provides: "Layout with Header/Footer integration"
      contains: "import Header"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Header.astro"
      via: "import"
      pattern: "import Header from"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Footer.astro"
      via: "import"
      pattern: "import Footer from"
    - from: "src/components/Header.astro"
      to: "/"
      via: "logo link"
      pattern: 'href="/"'
---

<objective>
Create Header and Footer Astro components and integrate them into BaseLayout for consistent navigation across all pages.

Purpose: Establish site-wide navigation structure with zero JavaScript, using pure HTML/CSS rendering.
Output: Header.astro, Footer.astro, updated BaseLayout.astro
</objective>

<execution_context>
@/home/mcrowe/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-components/02-RESEARCH.md
@.backup-react/src/app/components/Header/Header.tsx
@.backup-react/src/app/components/Footer/Footer.tsx
@src/layouts/BaseLayout.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header.astro component</name>
  <files>src/components/Header.astro</files>
  <action>
Create src/components/Header.astro as a zero-JS Astro component with:

1. Props interface accepting optional `currentPath?: string` for active link highlighting
2. Navigation items array matching original React component:
   - Home: /home
   - About: /about
   - Embed: /embed
   - Terms: /terms
   - Privacy: /privacy
   - Sites: /sites
3. Logo using the inline base64 PNG from original Header.tsx (copy exact data URI)
4. Tailwind CSS styling:
   - Header container: `bg-white shadow-sm` or similar
   - Logo: `navbar-brand` styling via Tailwind equivalents
   - Nav links: flex layout with proper spacing
   - Active link: `class:list` with conditional `font-bold` or `text-blue-600` for current path
5. Link separators: Use " / " between links as in original

DO NOT use client:* directives - this is a static component.
DO NOT import React or use React patterns.
DO use `class:list` for conditional active state styling.
</action>
  <verify>
echo "=== Header.astro exists ===" && test -f src/components/Header.astro && echo "PASS" || echo "FAIL"
echo "=== Contains logo data URI ===" && grep -q "data:image/png;base64" src/components/Header.astro && echo "PASS" || echo "FAIL"
echo "=== Has navigation items ===" && grep -q "/home" src/components/Header.astro && grep -q "/about" src/components/Header.astro && echo "PASS" || echo "FAIL"
echo "=== Uses Tailwind classes ===" && grep -q "class=" src/components/Header.astro && echo "PASS" || echo "FAIL"
</verify>
  <done>
Header.astro exists with inline logo, navigation links, Tailwind styling, and optional currentPath prop for active state.
</done>
</task>

<task type="auto">
  <name>Task 2: Create Footer.astro component</name>
  <files>src/components/Footer.astro</files>
  <action>
Create src/components/Footer.astro as a zero-JS Astro component with:

1. Dynamic year using `new Date().getFullYear()` in frontmatter
2. Footer links matching original React component:
   - Home: /home
   - About: /about
   - Embed: /embed
   - Terms: /terms
   - Privacy: /privacy
   - Sites: /sites (no trailing separator)
3. Link separators: " / " between links as in original
4. Center section: "Factual rankings via Media Bias/Fact Check" linking to https://mediabiasfactcheck.com
5. Right section: Copyright with dynamic year, link to /home for domain name
6. Tailwind CSS styling:
   - Footer container: `bg-gray-100 border-t` or similar (replaces bg-light)
   - Three-column grid layout on md+: `grid md:grid-cols-3`
   - Links styled with hover states
   - Proper text-muted equivalent: `text-gray-600`

DO NOT use client:* directives - this is a static component.
DO NOT use React Link component - use standard <a> tags.
</action>
  <verify>
echo "=== Footer.astro exists ===" && test -f src/components/Footer.astro && echo "PASS" || echo "FAIL"
echo "=== Has dynamic year ===" && grep -q "getFullYear" src/components/Footer.astro && echo "PASS" || echo "FAIL"
echo "=== Has MBFC link ===" && grep -q "mediabiasfactcheck.com" src/components/Footer.astro && echo "PASS" || echo "FAIL"
echo "=== Has copyright ===" && grep -q "copyright\|All Rights Reserved" src/components/Footer.astro && echo "PASS" || echo "FAIL"
</verify>
  <done>
Footer.astro exists with navigation links, MBFC attribution, dynamic copyright year, and Tailwind grid layout.
</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Header/Footer into BaseLayout</name>
  <files>src/layouts/BaseLayout.astro, src/pages/index.astro</files>
  <action>
Update src/layouts/BaseLayout.astro to include Header and Footer:

1. Import Header and Footer components at top of frontmatter
2. Add `currentPath?: string` to Props interface
3. Destructure currentPath from Astro.props with default '/'
4. Add Header before `<main>` with `currentPath={Astro.url.pathname}`
5. Add Footer after `<main>`
6. Wrap main content with appropriate padding/margin for fixed footer

Structure:
```astro
<body>
  <Header currentPath={Astro.url.pathname} />
  <main class="pb-16">
    <slot />
  </main>
  <Footer />
</body>
```

Update src/pages/index.astro to add title/description props if not already present.

DO verify build succeeds after changes.
</action>
  <verify>
echo "=== BaseLayout imports Header ===" && grep -q "import Header" src/layouts/BaseLayout.astro && echo "PASS" || echo "FAIL"
echo "=== BaseLayout imports Footer ===" && grep -q "import Footer" src/layouts/BaseLayout.astro && echo "PASS" || echo "FAIL"
echo "=== Header receives currentPath ===" && grep -q "currentPath={Astro.url.pathname}" src/layouts/BaseLayout.astro && echo "PASS" || echo "FAIL"
echo "=== Build succeeds ===" && npm run build 2>&1 | tail -5
</verify>
  <done>
BaseLayout includes Header with currentPath prop and Footer. Build succeeds with zero-JS components.
</done>
</task>

</tasks>

<verification>
1. Run `npm run build` - must succeed with zero errors
2. Check dist/index.html contains header content with logo
3. Check dist/index.html contains footer content with MBFC link
4. Verify no JavaScript hydration scripts for Header/Footer in build output
5. Run `npx astro check` - must pass with zero errors
</verification>

<success_criteria>
- Header.astro created with logo, navigation links, and active state support
- Footer.astro created with links, MBFC attribution, and dynamic copyright year
- BaseLayout.astro integrates both components with proper slot composition
- Build succeeds with zero-JS output for navigation components
- Home page renders with header and footer visible
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-components/02-01-SUMMARY.md`
</output>
