---
phase: 02-static-components
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/data/biases.json
  - src/data/hosts.json
  - src/components/Biases.astro
  - src/components/Hosts.astro
  - src/components/Sites.astro
  - src/pages/sites.astro
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can view bias categories with names and descriptions displayed"
    - "User can view site listing with site names and bias labels"
    - "Bias categories satire, fake-news, and conspiracy are filtered out"
    - "Hosts without labels are not displayed"
    - "No JavaScript is shipped for these components"
  artifacts:
    - path: "src/data/biases.json"
      provides: "Bias categories data at build time"
      min_lines: 50
    - path: "src/data/hosts.json"
      provides: "Hosts data at build time"
      min_lines: 100
    - path: "src/components/Biases.astro"
      provides: "Bias ratings display component"
      contains: "import.*biases.json"
    - path: "src/components/Hosts.astro"
      provides: "Sites listing table component"
      contains: "import.*hosts.json"
    - path: "src/components/Sites.astro"
      provides: "Combined Biases + Hosts wrapper"
      contains: "import.*Biases"
    - path: "src/pages/sites.astro"
      provides: "Sites page using Sites component"
  key_links:
    - from: "src/components/Biases.astro"
      to: "src/data/biases.json"
      via: "import"
      pattern: "import.*biases.*from"
    - from: "src/components/Hosts.astro"
      to: "src/data/hosts.json"
      via: "import"
      pattern: "import.*hosts.*from"
    - from: "src/components/Sites.astro"
      to: "src/components/Biases.astro"
      via: "import"
      pattern: "import.*Biases"
    - from: "src/pages/sites.astro"
      to: "src/components/Sites.astro"
      via: "import"
---

<objective>
Create Biases, Hosts, and Sites Astro components with build-time JSON data imports, and add a Sites page to display them.

Purpose: Migrate data display components to zero-JS Astro components with static data bundling.
Output: src/data/ directory with JSON files, Biases.astro, Hosts.astro, Sites.astro, sites.astro page
</objective>

<execution_context>
@/home/mcrowe/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-components/02-RESEARCH.md
@.planning/phases/02-static-components/02-01-PLAN.md
@.backup-react/src/app/components/Biases/Biases.tsx
@.backup-react/src/app/components/Hosts/Hosts.tsx
@.backup-react/src/app/components/Sites/Sites.tsx
@public/assets/biases.json
@public/assets/hosts.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/data/ directory and move JSON files</name>
  <files>src/data/biases.json, src/data/hosts.json</files>
  <action>
Create src/data/ directory and move JSON data files from public/assets/ to src/data/ for build-time bundling:

1. Create directory: `mkdir -p src/data`
2. Copy biases.json: `cp public/assets/biases.json src/data/biases.json`
3. Copy hosts.json: `cp public/assets/hosts.json src/data/hosts.json`
4. Do NOT delete files from public/assets/ yet (keep as backup during migration)

IMPORTANT: Files in src/data/ are processed by Vite at build time and bundled.
Files in public/ are served as-is without processing.

DO move files to src/data/ for type-safe imports.
DO NOT use fetch() - use static imports instead.
</action>
  <verify>
echo "=== src/data directory exists ===" && test -d src/data && echo "PASS" || echo "FAIL"
echo "=== biases.json in src/data ===" && test -f src/data/biases.json && echo "PASS" || echo "FAIL"
echo "=== hosts.json in src/data ===" && test -f src/data/hosts.json && echo "PASS" || echo "FAIL"
echo "=== biases.json is valid JSON ===" && node -e "JSON.parse(require('fs').readFileSync('src/data/biases.json'))" && echo "PASS" || echo "FAIL"
echo "=== hosts.json is valid JSON ===" && node -e "JSON.parse(require('fs').readFileSync('src/data/hosts.json'))" && echo "PASS" || echo "FAIL"
</verify>
  <done>
src/data/ directory exists with biases.json and hosts.json ready for build-time imports.
</done>
</task>

<task type="auto">
  <name>Task 2: Create Biases.astro component</name>
  <files>src/components/Biases.astro</files>
  <action>
Create src/components/Biases.astro as a zero-JS Astro component with:

1. Import biases.json from src/data/: `import biasesData from '../data/biases.json'`
2. Define TypeScript interface for BiasCategory (name, description, url)
3. Filter out satire, fake-news, conspiracy keys as in original React component:
   ```js
   const biasCategories = Object.entries(biasesData)
     .filter(([key]) => !['satire', 'fake-news', 'conspiracy'].includes(key))
     .map(([, value]) => value as BiasCategory);
   ```
4. Render as definition list with Tailwind grid:
   - Container: `container mx-auto px-4 py-8`
   - Title: `<h2>Media Bias Categories</h2>` or similar
   - Grid: `<dl class="grid md:grid-cols-2 gap-4">`
   - Name: `<dt class="font-semibold">`
   - Description: `<dd class="text-gray-700">`

DO NOT use fetch() - import JSON statically.
DO NOT use client:* directives - this is a static component.
DO filter out satire, fake-news, conspiracy as per original behavior.
</action>
  <verify>
echo "=== Biases.astro exists ===" && test -f src/components/Biases.astro && echo "PASS" || echo "FAIL"
echo "=== Imports biases.json ===" && grep -q "import.*biases.*from.*data" src/components/Biases.astro && echo "PASS" || echo "FAIL"
echo "=== Filters satire/fake-news/conspiracy ===" && grep -q "satire.*fake-news.*conspiracy" src/components/Biases.astro && echo "PASS" || echo "FAIL"
echo "=== Uses Tailwind grid ===" && grep -q "grid" src/components/Biases.astro && echo "PASS" || echo "FAIL"
</verify>
  <done>
Biases.astro exists with static JSON import, category filtering, and Tailwind-styled definition list.
</done>
</task>

<task type="auto">
  <name>Task 3: Create Hosts.astro component</name>
  <files>src/components/Hosts.astro</files>
  <action>
Create src/components/Hosts.astro as a zero-JS Astro component with:

1. Import hosts.json from src/data/: `import hostsData from '../data/hosts.json'`
2. Define TypeScript interface for Host (name, url, labels array)
3. Filter hosts with labels: `hostsData.filter(host => host.labels && host.labels.length > 0)`
4. Format bias label function:
   ```js
   const formatBiasLabel = (label: string) => {
     return label
       .replace(/-/g, ' ')
       .replace(/ only/g, '')
       .split(' ')
       .map(word => word.charAt(0).toUpperCase() + word.slice(1))
       .join('-');
   };
   ```
5. Render as table with Tailwind:
   - Container: `container mx-auto px-4 py-8`
   - Title: `<h4>Sites Available</h4>`
   - Table: `min-w-full border-collapse`
   - Header row with Site and Bias columns
   - Body with links (target="_blank" rel="noopener noreferrer")
   - Hover state on rows: `hover:bg-gray-50`
6. Total count at bottom: `<p>Total sites: {hosts.length}</p>`

DO NOT use fetch() - import JSON statically.
DO NOT use client:* directives - this is a static component.
DO format labels matching original React component behavior.
</action>
  <verify>
echo "=== Hosts.astro exists ===" && test -f src/components/Hosts.astro && echo "PASS" || echo "FAIL"
echo "=== Imports hosts.json ===" && grep -q "import.*hosts.*from.*data" src/components/Hosts.astro && echo "PASS" || echo "FAIL"
echo "=== Filters by labels ===" && grep -q "host.labels" src/components/Hosts.astro && echo "PASS" || echo "FAIL"
echo "=== Has table element ===" && grep -q "<table" src/components/Hosts.astro && echo "PASS" || echo "FAIL"
echo "=== Has external links ===" && grep -q 'target="_blank"' src/components/Hosts.astro && echo "PASS" || echo "FAIL"
</verify>
  <done>
Hosts.astro exists with static JSON import, label filtering, and Tailwind-styled table with external links.
</done>
</task>

<task type="auto">
  <name>Task 4: Create Sites.astro and sites page</name>
  <files>src/components/Sites.astro, src/pages/sites.astro</files>
  <action>
Create src/components/Sites.astro as a wrapper component:

1. Import Biases and Hosts components
2. Render both with spacing between:
   ```astro
   <div class="sites">
     <Biases />
     <div class="py-8">&nbsp;</div>
     <Hosts />
   </div>
   ```

Create src/pages/sites.astro page:

1. Use BaseLayout with title and description
2. Import and render Sites component:
   ```astro
   ---
   import BaseLayout from '../layouts/BaseLayout.astro';
   import Sites from '../components/Sites.astro';
   ---
   <BaseLayout title="Sites - Factual News Search" description="View all available news sources and their bias ratings">
     <Sites />
   </BaseLayout>
   ```

DO wrap content with BaseLayout.
DO provide meaningful title and description for SEO.
</action>
  <verify>
echo "=== Sites.astro component exists ===" && test -f src/components/Sites.astro && echo "PASS" || echo "FAIL"
echo "=== Sites imports Biases and Hosts ===" && grep -q "Biases" src/components/Sites.astro && grep -q "Hosts" src/components/Sites.astro && echo "PASS" || echo "FAIL"
echo "=== sites.astro page exists ===" && test -f src/pages/sites.astro && echo "PASS" || echo "FAIL"
echo "=== sites page uses BaseLayout ===" && grep -q "BaseLayout" src/pages/sites.astro && echo "PASS" || echo "FAIL"
echo "=== Build succeeds ===" && npm run build 2>&1 | tail -5
</verify>
  <done>
Sites.astro component wraps Biases and Hosts. sites.astro page exists with proper layout and SEO metadata.
</done>
</task>

</tasks>

<verification>
1. Run `npm run build` - must succeed with zero errors
2. Check dist/sites/index.html exists and contains bias categories
3. Check dist/sites/index.html contains hosts table with links
4. Verify no JavaScript files generated for Biases/Hosts/Sites components
5. Run `npx astro check` - must pass with zero errors
6. Verify filtering: dist/sites/index.html should NOT contain "satire", "fake-news", or "conspiracy" bias names
</verification>

<success_criteria>
- src/data/ directory contains biases.json and hosts.json
- Biases.astro imports JSON statically and filters categories correctly
- Hosts.astro imports JSON statically and displays filtered hosts table
- Sites.astro combines Biases and Hosts with proper spacing
- sites.astro page renders with BaseLayout and SEO metadata
- Build produces static HTML with no JavaScript for these components
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-components/02-02-SUMMARY.md`
</output>
