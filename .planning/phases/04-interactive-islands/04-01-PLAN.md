---
phase: 04-interactive-islands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/islands/Search.svelte
  - src/pages/index.astro
autonomous: true

must_haves:
  truths:
    - "User can see the search box immediately on page load"
    - "User can type a search query and get results from curated news sources"
    - "Google CSE script loads without console errors"
  artifacts:
    - path: "src/components/islands/Search.svelte"
      provides: "Google CSE search island"
      min_lines: 30
    - path: "src/pages/index.astro"
      provides: "Home page using Search island"
      contains: "Search client:load"
  key_links:
    - from: "src/pages/index.astro"
      to: "Search.svelte"
      via: "import and client:load directive"
      pattern: "Search client:load"
---

<objective>
Create Search.svelte island component that integrates Google Custom Search Engine for searching curated news sources.

Purpose: Restore search functionality from original React app using Svelte 5 with proper client-side hydration.
Output: Search.svelte component that loads Google CSE script on mount and renders the search widget.
</objective>

<execution_context>
@/home/mcrowe/.claude/get-shit-done/workflows/execute-plan.md
@/home/mcrowe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interactive-islands/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Search.svelte island component</name>
  <files>src/components/islands/Search.svelte</files>
  <action>
Create the Search.svelte component in src/components/islands/ directory.

Implementation details:
1. Use Svelte 5 with TypeScript (`<script lang="ts">`)
2. Import `onMount` from 'svelte' for lifecycle
3. Define CSE_ID constant: `'011275290256739755566:cwfn9qhuqkk'` (from original app.constants.ts)
4. In onMount:
   - Check if Google CSE already loaded (`window.google?.search?.Cse`)
   - If not, create script element with src `https://cse.google.com/cse.js?cx=${CSE_ID}`
   - Set async=true, append to document.head
   - Return cleanup function to remove script on unmount
5. Use `$state` for `loaded` boolean (set true when script loads or already exists)
6. Render:
   - `<main role="main" class="search">` wrapper
   - `<h2>Search News Sites with Highly Factual Reporting:</h2>`
   - Loading indicator when not loaded
   - `<div class="gcse-search" data-defaultToRefinement="mostly-center"></div>` for CSE widget

DO NOT use React patterns (useEffect, useState). Use Svelte 5 runes ($state) and onMount.
DO NOT use dangerouslySetInnerHTML - the gcse-search div works without it.
</action>
  <verify>
Check component exists and has correct structure:
- `ls src/components/islands/Search.svelte` confirms file exists
- File contains `onMount`, `$state`, `gcse-search`, and CSE ID
</verify>
  <done>
Search.svelte exists with Google CSE integration using Svelte 5 patterns, proper script loading in onMount, and cleanup function.
</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Search island into home page</name>
  <files>src/pages/index.astro</files>
  <action>
Update the home page (index.astro) to use the Search island.

Implementation:
1. Import Search component: `import Search from '../components/islands/Search.svelte'`
2. Add `<Search client:load />` in the main content area
3. Ensure BaseLayout wraps the page
4. Set appropriate page title and description in frontmatter

The `client:load` directive ensures immediate interactivity for the primary search function.

If index.astro doesn't exist yet (Phase 3 not complete), create a minimal version:
- Use BaseLayout
- Include Search component with client:load
- Basic page structure
</action>
  <verify>
- `cat src/pages/index.astro | grep "Search client:load"` confirms integration
- Build succeeds: `npm run build`
</verify>
  <done>
Home page includes Search.svelte with client:load directive. Build completes without errors.
</done>
</task>

<task type="auto">
  <name>Task 3: Verify search functionality</name>
  <files>src/components/islands/Search.svelte</files>
  <action>
Run development server and verify search works:
1. Start dev server: `npm run dev`
2. Open browser to localhost:4321
3. Verify:
   - Search box appears immediately on load
   - No console errors related to Google CSE
   - Typing a query triggers search results from curated sources

Then run build to ensure production works:
1. `npm run build`
2. Preview build: `npm run preview`
3. Verify search still works in production build

Document any issues found during verification.
</action>
  <verify>
- Dev server starts without errors
- Search widget renders on home page
- Build completes successfully
- Preview server serves functional search
</verify>
  <done>
Search functionality verified working in both dev and production builds. No console errors. Google CSE loads and returns results.
</done>
</task>

</tasks>

<verification>
- [ ] Search.svelte exists in src/components/islands/
- [ ] Component uses Svelte 5 patterns ($state, onMount)
- [ ] CSE ID matches original: 011275290256739755566:cwfn9qhuqkk
- [ ] index.astro imports Search with client:load
- [ ] Build succeeds (npm run build)
- [ ] Search widget renders and is functional
</verification>

<success_criteria>
Search functionality fully restored:
- User can see search box immediately on page load (client:load)
- User can search curated news sources via Google CSE
- No React dependencies in search island
- Proper cleanup when navigating away
</success_criteria>

<output>
After completion, create `.planning/phases/04-interactive-islands/04-01-SUMMARY.md`
</output>
